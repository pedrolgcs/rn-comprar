import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    gap: 16,
    alignItems: 'center',
    backgroundColor: '#f8fafc',
    padding: 24
  },
  logo: {
    height: 34,
    width: 134,
  }
});
